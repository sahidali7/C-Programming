// Example of 'for Loop' for increment operator
// i++ and ++i

#include<stdio.h>

int main() {
    int i = 1;

    printf("%d\n", i++); // first print i and then increase, it will print 1 only because there is no other printf statement

    return 0;
}