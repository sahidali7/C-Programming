// Exam of 'for Loop' for Loop counter can  be float or even number

#include<stdio.h>

int main() {
    for(char ch = 'a'; ch <= 'z'; ch++) {
        printf("%c\n",ch);
    }
    return 0;
}